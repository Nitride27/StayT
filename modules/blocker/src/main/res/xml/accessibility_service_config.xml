<?xml version="1.0" encoding="utf-8"?>
<!-- canRetrieveWindowContent=true is REQUIRED for event.getSource() (the
     feed-shield EditText guard) — without it the source is always null.
     It (plus flagRetrieveInteractiveWindows) is also required for
     getWindows(): the split-screen / pop-up / PiP window scan (ADR-0009). -->
<accessibility-service xmlns:android="http://schemas.android.com/apk/res/android"
    android:description="@string/accessibility_service_description"
    android:accessibilityEventTypes="typeWindowStateChanged|typeWindowsChanged|typeWindowContentChanged"
    android:accessibilityFlags="flagIncludeNotImportantViews|flagReportViewIds|flagRetrieveInteractiveWindows"
    android:accessibilityFeedbackType="feedbackGeneric"
    android:notificationTimeout="100"
    android:canRetrieveWindowContent="true"
    android:settingsActivity="com.nitridee.staytapp.MainActivity" />

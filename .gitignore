# Learn more https://docs.github.com/en/get-started/getting-started-with-git/ignoring-files

# dependencies
node_modules/

# Expo
.expo/
dist/
web-build/
expo-env.d.ts

# Native
.kotlin/
*.orig.*
*.jks
*.p8
*.p12
*.key
*.mobileprovision

# Metro
.metro-health-check*

# debug
npm-debug.*
yarn-debug.*
yarn-error.*

# macOS
.DS_Store
*.pem

# local env files
.env*.local

# typescript
*.tsbuildinfo

# generated native folders
/ios
# D2: android/ is generated by `expo prebuild` from modules/blocker/ + the
# Expo template (see modules/blocker/plugin.js). Never commit it — the
# committed mirror drifted before (missing deep-link filter, hand-added
# strings). EAS regenerates via eas-build-post-install; local dev via
# `expo run:android`. modules/blocker/ is the single native source.
/android/

# graphify analysis output
/graphify-out
stayt-v4.apk
Context.md
